# Copy to .env (gitignored) and fill in real values. Loaded automatically by
# aslcv.generator.llm_feedback for `diagnose_demo.py --llm-feedback`.

# https://huggingface.co/settings/tokens -- a "Read" token is enough.
HF_TOKEN=
